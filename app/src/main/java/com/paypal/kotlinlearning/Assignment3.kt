package com.paypal.kotlinlearning

/*
Create higher order function
    Input - 1. nullable string
            2. function returning count of char except space
                (String) -> Int

execute function via lambda - trailing / it
 */